<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crowdvoice.app">

    <!--
        Permisos necesarios:

        INTERNET: No necesario para el MVP (TTS es local),
                  pero reservado para futura integración de TTS en la nube.

        WRITE_EXTERNAL_STORAGE: Requerido solo en Android 9 y anteriores
        para guardar el archivo WAV en la carpeta Música.
        En Android 10+ se usa MediaStore API sin permiso.

        RECORD_AUDIO: No necesario actualmente.
        Reservado para futura función de grabación de voz real.
    -->

    <!-- Guardar audio en Android ≤ 9 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!-- FUTURO: Grabación de voz del usuario -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO"/> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.CrowdVoice"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            FUTURO: Servicio de generación en segundo plano
            <service android:name=".CrowdGenerationService" />
        -->

    </application>

</manifest>
