workflows:
  android-build:
    name: Android Build
    max_build_duration: 30

    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk